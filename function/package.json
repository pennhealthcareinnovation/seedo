{
  "name": "@seedo/function",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "cd ../server && nest build && cd ../function && tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@azure/functions": "^3.2.0",
    "@nestjs/common": "^8.0.0",
    "@nestjs/config": "^2.1.0",
    "@nestjs/core": "^8.0.0",
    "@seedo/server": "file:../server"
  },
  "devDependencies": {
    "typescript": "^4.0.0"
  }
}

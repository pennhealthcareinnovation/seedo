{
  "name": "@seedo/function",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "cd ../server && npm run build:nest && cd ../function && tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@azure/functions": "^3.2.0",
    "@nestjs/common": "^9.0.11",
    "@nestjs/config": "^2.1.0",
    "@nestjs/core": "^9.0.11",
    "@seedo/server": "file:../server"
  },
  "devDependencies": {
    "@nestjs/cli": "^9.1.1",
    "typescript": "^4.3.5"
  }
}
